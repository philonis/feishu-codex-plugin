{
  "name": "feishu-codex-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Optional per-session bridge between Feishu bot chats and local Codex CLI sessions.",
  "scripts": {
    "start": "node src/bridge.mjs run",
    "bridge": "node src/bridge.mjs",
    "install:prompts": "node src/install-prompts.mjs",
    "status": "node src/bridge.mjs status"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.62.1"
  }
}
