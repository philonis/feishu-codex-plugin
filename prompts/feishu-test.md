---
description: "Send a Feishu bridge test message"
argument-hint: ""
---
Send a Feishu bridge test message to the configured default chat.

Run:

```bash
npm --prefix __PLUGIN_DIR__ run bridge -- send-test
```

Then briefly report whether the message was sent.
