---
description: "Show Feishu Codex bridge status"
argument-hint: ""
---
Show the Feishu Codex bridge status.

Run:

```bash
npm --prefix __PLUGIN_DIR__ run status
```

Then summarize whether the bridge is active, which session is attached, and which Feishu chat is attached.
